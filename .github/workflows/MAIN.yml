# Nombre del workflow, se mostrará en la pestaña de Actions de tu repo
name: Generate Snake

# Controla cuándo se ejecutará el workflow
on:
  # Se ejecutará a las 00:00 UTC todos los días
  schedule:
    - cron: "0 0 * * *"
  # También te permite ejecutarlo manualmente desde la pestaña de Actions
  workflow_dispatch:

# Permisos que necesita el workflow para funcionar
permissions:
  contents: write

jobs:
  # Define el trabajo a realizar
  build:
    # Se usará la última versión de Ubuntu como entorno
    runs-on: ubuntu-latest
    
    # Pasos que se ejecutarán en el trabajo
    steps:
      # Paso 1: Descarga el código de tu repositorio
      - name: Checkout repository
        uses: actions/checkout@v4

      # Paso 2: Genera la animación de la serpiente
      - name: Generate snake.svg
        uses: Platane/snk/svg-only@v3
        id: snake-svg
        with:
          # Tu nombre de usuario de GitHub
          github_user_name: ${{ github.repository_owner }}
          # Los outputs que se generarán
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
          # Opcional: paleta de colores para la serpiente
            snake_color: green
          # Opcional: paleta de colores para las "manzanas"
            apple_color: red

      # Paso 3: Sube el archivo SVG generado al repositorio
      - name: Push snake.svg to the output branch
        uses: EndBug/add-and-commit@v9
        with:
          # El token que guardamos como secreto
          token: ${{ secrets.GH_TOKEN }}
          # El mensaje del commit que se creará
          message: "🐍 Actualizar animación de la serpiente"
          # La rama donde se guardará el archivo (normalmente la principal)
          branch: main
          # Los archivos a añadir al commit
          add: 'dist/*.svg'
